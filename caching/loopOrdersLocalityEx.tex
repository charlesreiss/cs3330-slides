\begin{frame}[fragile,label=loopOrderLocalEx{loop orders and locality}
\begin{lstlisting}[language=C++,style=small]
for (int i = 0; i < N; ++i)
    for (int j = 0; j < N; ++i)
        A[i * N] = B[j];
    \end{lstlisting}
\end{frame}
